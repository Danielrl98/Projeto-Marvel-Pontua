import styled from "styled-components";
import theme from "../theme/theme";

export const MessageErrorComponent = styled.p`
   color:  ${theme.color.orange700} !important;
   font-family: ${theme.fonts.epilogue};
   font-size: 16px;
   margin-top:6px;
   margin-bottom:6px;
   text-align:center
`
